Problema 2
Write some code to prompt a user to enter two boolean values, A and B.
The user might type "t" or "yes" instead of "true", though, so start by determining if each of the values is true or false.
True values might be "true", "t", "y", or "1", while everything else is considered false. Once you know the two values,
print the result of "A && B".


Declare A_llegit, B_llegit, A, B, resultat as boolean

print "enter boolean value for A"

A_llegit = read_input

if A_llegit = "true" or "t" or "y" or "1" then
  A = "t"
else
  A = 'f'
end_if 

print "enter boolean value for B"

B_llegit = read_input

If B_llegit = "true" or "t" or "y" or "1" then
  B = "t"
else
  B = 'f'
end_if

if A = "t" i B = "t" then
  resultat = "t"
else
  resultat = "f"
end-if

print resultat
